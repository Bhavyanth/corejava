package com.oops.interface_class;

public interface Bank {
	/**
	 * Methods of interfaces are abstract by default
	 * 
	 * Interface can only be implemented not extended
	 * 
	 * if class and class -> use extends
	 * if interface and class -> use implements
	 * if interface and interface -> use extends
	 */
	int getInterestRate();
}
